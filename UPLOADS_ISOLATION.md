# 上传文件隔离说明

本文档说明项目中上传文件的隔离机制，确保安全性和可维护性。

## 目录

- [隔离策略](#隔离策略)
- [目录结构](#目录结构)
- [安全措施](#安全措施)
- [清理机制](#清理机制)

---

## 隔离策略

### 为什么需要文件隔离？

1. **安全性**: 防止上传的恶意文件访问系统资源
2. **可维护性**: 便于管理和清理孤立文件
3. **性能**: 优化存储空间使用
4. **隐私**: 保护用户上传的内容

### 隔离原则

- 用户上传的文件与系统文件完全分离
- 临时文件与永久文件分开存储
- 每个文章的图片独立管理

---

## 目录结构

```
server/
├── public/
│   ├── uploads/           # 用户上传的文件
│   │   ├── images/        # 图片文件
│   │   └── temp/          # 临时文件（定期清理）
│   └── .gitignore         # 忽略上传目录
└── data/
    └── posts/             # 文章Markdown文件
```

### 目录说明

- **public/uploads/images/**: 正式上传的图片，与文章关联
- **public/uploads/temp/**: 临时上传的文件，未关联到文章
- **data/posts/**: 文章内容存储（Markdown格式）

---

## 安全措施

### 文件类型限制

只允许上传以下文件类型：

- 图片: `.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`
- 文档: `.pdf`, `.md`

### 文件大小限制

- 单个文件最大 10MB
- 总上传空间可配置

### 访问控制

- 上传目录不可执行脚本
- 文件名重命名避免冲突
- 路径遍历防护

---

## 清理机制

### 自动清理

系统会定期清理孤立的文件：

1. **识别孤立文件**: 检查uploads目录中未被任何文章引用的文件
2. **临时文件清理**: 删除超过指定时间的临时文件
3. **安全删除**: 确保只删除未被引用的文件

### 手动清理

管理员可以手动触发清理：

```bash
# 进入后端目录
cd server

# 运行清理脚本
python -m app.services.image_cleanup
```

### 清理策略

- 临时文件：超过24小时自动删除
- 孤立文件：超过7天且无引用自动删除
- 已删除文章的图片：文章删除后标记为孤立

---

## 配置选项

在 `server/.env` 中可以配置：

```env
# 上传文件配置
MAX_UPLOAD_SIZE=10485760  # 10MB
UPLOAD_DIR=/app/public/uploads
TEMP_UPLOAD_DIR=/app/public/uploads/temp
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,webp,pdf,md

# 清理配置
TEMP_FILE_TTL=86400  # 24小时（秒）
ORPHAN_FILE_TTL=604800  # 7天（秒）
```

---

## 监控和维护

### 监控指标

- 上传文件数量
- 存储空间使用
- 孤立文件数量
- 临时文件数量

### 定期维护

建议每周执行一次完整检查：

```bash
# 检查磁盘使用
df -h

# 检查上传目录大小
du -sh server/public/uploads

# 查看孤立文件
docker compose exec backend python -m app.services.image_cleanup --dry-run
```

---

## 故障排查

### 问题：文件上传失败

**可能原因:**
- 文件大小超过限制
- 文件类型不允许
- 磁盘空间不足

**解决方案:**
- 检查 `.env` 配置
- 查看服务器日志
- 确认目录权限

### 问题：孤立文件未清理

**可能原因:**
- 清理服务未运行
- 文件仍在被引用
- TTL配置过长

**解决方案:**
- 手动运行清理脚本
- 检查文章引用
- 调整TTL配置

---

## 最佳实践

1. **定期备份**: 重要上传内容定期备份
2. **监控空间**: 设置磁盘使用告警
3. **权限管理**: 严格限制上传目录权限
4. **日志记录**: 记录所有上传和删除操作
5. **安全扫描**: 定期扫描上传的恶意文件
